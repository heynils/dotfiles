local options = {
  ensure_installed = { "lua", "c_sharp", "csv", "json", "rust"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
